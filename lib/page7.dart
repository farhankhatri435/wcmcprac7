import 'package:flutter/material.dart';

class Page7 extends StatelessWidget {
  Widget build(BuildContext context) {
    return Center(
        child: Card(
            color: Colors.blue,
            child: Container(
                height: 200,
                width: 200,
                // color: Colors.blue,
                child: Center(child: Text('This is CardView')))));
  }
}
